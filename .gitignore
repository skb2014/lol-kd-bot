# don't push the .env file which contains the API keys and such
.env

# ignore the venv created by PyCharm
venv/
.venv/

# PyCharm-specific hidden configuration files
.idea/

# log files
*.log

# also don't want to push the most_recent_matches file
# because the file on the Pi will actually have the most recent matches
# and this one won't
most_recent_matches.json
